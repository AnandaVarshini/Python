from collections import Counter

s = input().strip()

# Count frequency of characters
count = Counter(s)

# Sort by frequency (descending) and character (ascending)
sorted_chars = sorted(count.items(), key=lambda x: (-x[1], x[0]))

# Print top 3
for char, freq in sorted_chars[:3]:
    print(char, freq)
